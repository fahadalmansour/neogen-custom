# NeoGen Snippets

Reusable PHP snippets deployed as a regular plugin (distinct from mu-plugins — can be disabled/enabled via WP admin).

Add new snippet files alongside this README and require them from `neogen-snippets.php`.
