.DS_Store
._*
*.log
*.swp
*.swo
.vscode/
.idea/
node_modules/
vendor/
*.env
.env
.env.*
!.env.example
*.secret
*.key
*.pem
wp-config.php
*.sql
*.sql.gz
*.zip
*.tar.gz

# Official certificates / PII documents — never stage
*.pdf
!brand/print/*.pdf

.claude/
.playwright-mcp/
.stfolder/
.stignore

# Personal productivity overlay (added by productivity:start, 2026-04-23)
# These are not deployable code — keep them local.
TASKS.md
CLAUDE-personal.md
dashboard.html
memory/

# Static brand-kit HTML exports + their _files/ — design references, not deployable code
NeoGen*.html
NEOGEN*.html
*_files/
الرئيسية*.html

# Local audit / screenshot artefacts
audit/
neogen-*.png
neogen-store-*.png
neogen-*-eval.json
ACTION-PLAN.md
FULL-AUDIT-REPORT.md

# Upstream agent-skills clone — not part of this repo
agent-skills/

# But the brand-kit PNGs anywhere under mu-plugins/.../img/ are deployable assets — not screenshots
!mu-plugins/neogen-theme-assets/img/**/*.png

# Brand asset suite (favicons, social cards, profile/banner artwork) — repo-tracked
!brand/**/*.png
!brand/**/*.svg
!brand/**/*.html

# Claude Code plugin packages (user-side authoring, not deploy artefacts)
*.plugin

# Skill / plan exploration outputs
outputs/

# Distributor auth-bridge session files — literal credentials, never commit.
*.auth.json
scripts/_auth/*.json
