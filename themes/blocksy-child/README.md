# Blocksy Child overrides

Files here are overlaid onto the existing `wp-content/themes/blocksy-child/` directory on the server.

Any theme file you want to override (header.php, footer.php, template-parts/*.php, style.css) goes here. On deploy, the `neogen-deploy` plugin rsyncs this folder into the live theme.
